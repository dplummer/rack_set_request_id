require 'spec_helper'

describe RackSetRequestId do
  it 'has a version number' do
    expect(RackSetRequestId::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
